# Real credentials - never commit or share this file.

# jobright.ai login
JOBRIGHT_EMAIL=medisettisurendra15@gmail.com
JOBRIGHT_PASSWORD="sm##01SM"

# Gmail account that will SEND the outreach emails.
GMAIL_ADDRESS=surendramedisetti99@gmail.com
GMAIL_APP_PASSWORD="ucfgvvzxmwcuydkp"

# Path to the resume PDF that gets attached to every outreach email
RESUME_PATH="Resumes/Surendra_Data Center.pdf"

# Search term and filters
SEARCH_TERM=Amazon Data Center

# Set to "true" to run the whole flow WITHOUT actually sending emails
# (still scrapes + logs to CSV). Good for checking selectors work
# before you fire real emails at real people.
DRY_RUN=true

# Stop the whole run once this many emails have been found/handled
# (sent, or would-be-sent in dry run). 0 = unlimited. Any job left
# mid-way is picked back up on the next run.
MAX_EMAILS=20

# --- Web app (server/) settings below ---

PORT=4000
JWT_SECRET=0c4d8501ed68995563ec6324a854d0f2b8e2088e7e2feba9f22f6d66e7c8621d
ENCRYPTION_KEY=1e72ff81f7a59f9399bdcb4ff2e7b32d1a4414749a21c9fce5fb3fa3d7b5f2cb
# DATA_DIR is resolved to an absolute path automatically (defaults to
# server/data); DATABASE_URL is derived from it, don't set both.
HEADLESS=true

# First-boot only: creates your admin login for the web app. Change this
# password after your first login; safe to delete these two lines afterward.
SEED_ADMIN_EMAIL=medisettisurendra517@gmail.com
SEED_ADMIN_PASSWORD="sm##01SM"
